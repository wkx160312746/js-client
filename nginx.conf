worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    access_log /var/log/nginx/access.log;
    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml application/javascript application/json application/xml;

    server {
        listen __PORT__;
        listen [::]:__PORT__;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        location = /health {
            access_log off;
            add_header Content-Type text/plain;
            return 200 "healthy\n";
        }

        # This file contains runtime deployment settings and must never be
        # served from a stale browser or CDN cache.
        location = /js/config.js {
            add_header Cache-Control "no-store, no-cache, must-revalidate" always;
            add_header Pragma "no-cache" always;
            expires -1;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Source files are not content-hashed, so browsers must revalidate them
        # after a deployment instead of keeping an old build for a year.
        location ~* \.(?:html|js|css)$ {
            expires -1;
            add_header Cache-Control "no-cache" always;
        }

        location ~* \.(?:png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header Vary Accept-Encoding;
            etag on;
        }
    }
}
